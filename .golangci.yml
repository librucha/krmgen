version: "2"

# Default linter set (errcheck, govet, ineffassign, staticcheck, unused).
# Nothing extra enabled — the repo passes cleanly against this set as of the
# first run golangci-lint ever had here; extend deliberately, not by accident.
linters:
  default: standard
