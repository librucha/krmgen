---
# Source: demo/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: fallback-release-demo
data:
  message: "from-env"
  replicas: "1"

---
# Source: demo/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: fallback-release-demo
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: fallback-release-demo

