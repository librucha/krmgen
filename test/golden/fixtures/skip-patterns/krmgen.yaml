apiVersion: krmgen.config.librucha.com/v1alpha1
kind: KrmGen
skip:
  - "*.pfx"
helm:
  charts:
    - name: demo
      repo: '{{ argocdEnv "CHART_REPO" }}'
      releaseName: rel
      version: 0.1.0
      ignoreCredentials: true
