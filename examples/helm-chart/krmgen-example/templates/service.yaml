apiVersion: v1
kind: Service
metadata:
  name: {{ include "krmgen-example.fullname" . }}
  labels:
    {{- include "krmgen-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "krmgen-example.selectorLabels" . | nindent 4 }}
